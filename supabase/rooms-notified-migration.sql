-- شغّل الملف ده مرة واحدة في Supabase SQL Editor.
--
-- جدول بسيط بيتتبع بس: "بعتنا إيميل تنبيه عن الطلب ده قبل كده ولا لأ؟" عشان نظام
-- التنبيهات بالإيميل ميبعتش نفس الإيميل كذا مرة كل ما الـ cron يشتغل.
-- الجدول ده مش بيسجل أي بيانات عن التاسك نفسه (نص، عميل، إلخ) - بس id التاسك ووقت الإرسال،
-- ده مختلف تمامًا عن سحب/تخزين بيانات المهام الفعلية.

create table if not exists rooms_notified_pending (
  task_id bigint primary key,
  notified_at timestamptz not null default now()
);

-- الجدول ده بيتقرا/بيتكتب بس من كود سيرفر داخلي (مفيش يوزر حقيقي وراه)،
-- فمش محتاج RLS زي باقي الجداول.
